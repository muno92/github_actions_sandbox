name: "Test"
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          gh api \
            -H "Accept: application/vnd.github+json" \
            "/repos/${GITHUB_ACTION_REPOSITORY}/actions/runs"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
