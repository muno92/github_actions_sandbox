name: "Test"
on:
  push:

jobs:
  test:
    strategy:
      os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: LC_ALL=fr_FR.UTF-8 date +'%A'
